## Binary Search Tree Trial

run a .txt input file to populate BST with key/value pairs.

u32 kays and 2 choices of value text or Point.

data/bst1.txt either 'geo' or 'text'

cargo run data/bst1.txt geo
