## Red Black balanced Tree

run a .txt input file to populate RBT with key/value pairs.

u32 keys and 2 choices of value text or Point.

bin/data/bst1.txt either 'geo' or 'text'

cargo run bin/data/bst1.txt geo
